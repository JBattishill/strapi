'use strict';

/**
 * nutrition-adjustment service.
 */

const { createCoreService } = require('@strapi/strapi').factories;

module.exports = createCoreService('api::nutrition-adjustment.nutrition-adjustment');
